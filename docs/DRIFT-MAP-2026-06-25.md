# Mapa de Drift — `main` vs Produção (2026-06-25)

> **Diagnóstico SOMENTE LEITURA.** Nada foi alterado: nenhuma branch real tocada,
> nenhum merge/push/deploy/migração, banco não foi escrito. Tudo só leitura.
> Worktree temporário (`/tmp/drift-check`) usado para simular o rebase do Bloco 1
> e **removido no fim**.

---

## 🎯 RESULTADO PRINCIPAL (a pergunta de prioridade máxima)

**Produção (`7823443`) é um SUPERCONJUNTO LIMPO de tudo.** A cadeia é perfeitamente
linear e nenhum deploy regrediu nada:

```
0cfb3f8 (main, 17/jun) ──ancestral──▶ 51776d6 (wa-ui-polish, 18/jun) ──ancestral──▶ 7823443 (PROD, 23/jun)
```

Provas (git, read-only):
- `merge-base --is-ancestor 0cfb3f8 51776d6` → ✅ verdadeiro
- `merge-base --is-ancestor 51776d6 7823443` → ✅ verdadeiro
- `merge-base(main, prod) == 0cfb3f8` (a própria main)
- `git log 7823443..0cfb3f8` (commits na main que faltam em prod) → **VAZIO**

**Conclusão:** produção contém 100% da main + WhatsApp UI polish + integra-lentes +
correções da rotina 21/06 + SKU. **Nada sumiu, nada regrediu.** A main só está
travada num ponto antigo da MESMA linha do tempo. Reconciliar é trivial.

---

## 1. Mapa das branches

| Data | Commit | Branch | O que carrega |
|------|--------|--------|---------------|
| 25/jun | `7b695ec` | **fix/seguranca-isolamento** | Bloco 1 (XSS recibo/carnê, IDOR conciliação, isolamento caixa) — saiu da main |
| 23/jun | `7823443` | **fix/rotina-teste-21-06** | **= PRODUÇÃO.** Rotina 21/06 + auditoria + SKU |
| 19/jun | `51776d6` | feat/integra-wa-ui-polish | WhatsApp UI polish + integra-lentes (deploy intermediário) |
| 18/jun | `ea8be98` | feat/integra-lentes | Assistente de Lentes F1-F4 + widget |
| 17/jun | `0cfb3f8` | **main** + feat/whatsapp-travas-config | **MAIN TRAVADA AQUI.** WhatsApp travas super admin |
| 17/jun | `4e00d1a` | feat/c1-medicao-ia-deploy | Lentes F2 + medição IA |
| 16/jun | `c9359e3` | feat/integra-anti-bloqueio-v2 | WhatsApp anti-bloqueio |
| 05/jun | `4dfda4c` | feature/cockpit-monitoramento | Painel cockpit (NÃO em prod — ver §5) |

(+ ~15 branches de feature mais antigas, todas ancestrais de prod.)

---

## 2. Gap de CÓDIGO (produção ↔ main)

### O que está em PROD mas NÃO na main (50 commits) — sumiria num deploy da main pelada:
Por feature (em português claro):
- **🔬 Assistente de Lentes (completo):** motor óptico determinístico, Fase 2 (base de
  conhecimento + playground + ligar/desligar IA em massa), Fase 3 (a IA por cima:
  lens-advisor.service + rota vendedor + botão na OS), Fase 4 (OCR de receita usa
  chave cifrada + modelo configurável), e o **widget flutuante global** (a "bolinha"
  com receita + IA + expiração 10min).
- **📱 WhatsApp UI polish:** card de boas-práticas, preview ao vivo no super admin,
  override por ótica, passos numerados.
- **🐛 Correções da rotina 21/06:** orçamento (inserir produto), importação (cliente
  desde), caixa negativo, fundo de troco herdado, baixa no caixa, impressão.
- **🔍 Correções da auditoria adversarial** (estorno por net, data -1 dia, etc.).
- **🏷️ UX do SKU** (PS VISION — sanitização do SKU).

### O que está na main mas NÃO em prod:
**NADA.** O reverse-gap (`git log 7823443..0cfb3f8`) é vazio. A main é um subconjunto
estrito de prod.

---

## 3. Gap de BANCO (migrações) — o mais perigoso

**Contagem:** main (`0cfb3f8`) = **53** migrations · prod (`7823443`) = **56** · banco
de produção = **56 aplicadas**.

### As 3 migrations que PROD tem e a main NÃO (todas já aplicadas no banco):

| # | Migration | O que faz | Aditiva? |
|---|-----------|-----------|----------|
| 1 | `20260617090000_lens_advisor_model` | `ALTER TABLE "AiGlobalConfig" ADD COLUMN "lensAdvisorModel" ... DEFAULT` | ✅ ADD COLUMN c/ default |
| 2 | `20260617100000_lens_knowledge` | `CREATE TABLE "LensKnowledgeDoc"` + `ALTER ... DROP NOT NULL` em AiTokenUsage.companyId | ✅ tabela nova + relaxa constraint |
| 3 | `20260617130000_ocr_model` | `ALTER TABLE "AiGlobalConfig" ADD COLUMN "ocrModel" ... DEFAULT` | ✅ ADD COLUMN c/ default |

**Todas 100% ADITIVAS** — nenhum DROP de tabela/coluna, nenhum DELETE/TRUNCATE,
nenhuma coluna nova NOT NULL sem default. **Risco de reaplicar = zero**, mas nem é
preciso reaplicar: o banco de prod **já as tem**. Quando a main alcançar `7823443`,
a pasta de migrations já incluirá as 3 e o banco já as terá → `migrate status` ficará
consistente **SEM rodar `migrate deploy`**.

### Migrations na branch que ainda não foram aplicadas no banco:
**Nenhuma.** `prisma migrate status` na branch atual: "Database schema is up to date"
(53 found, banco em dia com elas). Na branch de prod seriam 56 found, banco com 56 → em dia.

### ⚠️ Nuance do `migrate status`
Rodei na branch `fix/seguranca-isolamento` (base = main, 53 migrations). Ele disse
"up to date" porque o Prisma só compara as 53 da pasta com o histórico do banco — ele
**não alerta** sobre as 3 migrations extras que o banco tem além da pasta. Por isso a
contagem de pasta (53 vs 56) é o sinal real do gap, não o "up to date".

---

## 4. Conflitos

### (a) main → produção: **FAST-FORWARD PURO, zero conflito.**
`merge-base(main, prod) == main`, então `git branch -f main 7823443` é um avanço linear
puro (sem merge commit, sem conflito).

### (b) Bloco 1 (`fix/seguranca-isolamento`) sobre produção: **SEM CONFLITO.**
- 14 arquivos alterados pelo Bloco 1; **13 disjuntos** de prod.
- **1 arquivo tocado por ambos:** `src/services/cash.service.ts` (prod mexeu na
  auto-abertura/herança de fundo; Bloco 1 mexeu no `closeShift` — **regiões diferentes**).
- `git merge-tree --write-tree 7823443 7b695ec` → **EXIT 0 (limpo)**.
- Confirmado em worktree temporário descartável: `git cherry-pick 7b695ec` sobre prod →
  **EXIT 0**, e o `cash.service.ts` resultante contém **as duas** mudanças
  (`expectedBranchId` do Bloco 1 + `autoOpenShiftWithInheritedFloat` de prod). Worktree
  removido.

---

## 5. Decisões pendentes do MATHEUS (não decido sozinho)

### 🟡 Painel "Cockpit" (MetricSample / AdminActionLog)
- As migrations `20260605120000_add_metric_sample` e `..._add_admin_action_log` **já
  estão na main E em prod** (fazem parte das 53) e **aplicadas no banco**.
- **MAS** os models `MetricSample`/`AdminActionLog` **NÃO estão no `schema.prisma` de
  prod** — só existem na branch `feature/cockpit-monitoramento` (parada por decisão
  anterior).
- **Situação:** tabelas existem no banco, sem model no Prisma Client, sem painel.
  **Não bloqueia a reconciliação** (já é idêntico em main e prod).
- **Decisão do Matheus (futura, não urgente):** trazer os models do cockpit p/ o schema
  (alinhar Prisma↔banco) **ou** dropar as 2 tabelas órfãs **ou** deixar como está?
  Trade-off: alinhar = Prisma reflete o banco real; dropar = limpa tabela sem uso;
  deixar = drift Prisma↔banco persiste (inofensivo hoje). **Fora do escopo deste bloco.**

### 🟢 As 3 migrations de Lentes/OCR
- São features que **já estão em produção e funcionando** (Assistente de Lentes). Adotar
  na main é só formalizar o que já roda. **Recomendo adotar** (não há trade-off — é o
  estado real de prod). Decisão trivial, mas registro por completude.

---

## 🗺️ PLANO DE RECONCILIAÇÃO PROPOSTO (NÃO EXECUTADO — só proposta)

> **Nada disso foi executado.** É proposta para você e o Matheus aprovarem.

**Objetivo:** `main` = produção (`7823443`), depois Bloco 1 (segurança) rebaseado em
cima, testado e pronto p/ deploy — sem perder feature nem quebrar banco.

### Fase 0 — Preparação (checkpoint ⛔ aprovação do Matheus antes de começar)
0.1. Snapshot do Neon (banco de prod) ANTES de qualquer coisa — mesmo o plano não
     tocando migração, é a rede de segurança. (Matheus faz no painel Neon, ou autoriza.)
0.2. Tag de backup da main atual: `git tag backup/main-antes-reconciliacao-2026-06-25 0cfb3f8`.

### Fase 1 — Reconciliar main = prod (só código, FF puro, ZERO migração)
1.1. `git branch -f main 7823443` (fast-forward puro — provado sem conflito).
1.2. **NÃO rodar `migrate deploy`** — as 3 migrations já estão no banco; a pasta agora
     tem as 56, o banco tem as 56 → consistente.
1.3. ✅ Checkpoint: `git log main` mostra `7823443` no topo; `prisma migrate status`
     (agora com 56 na pasta) deve dizer "up to date". **PARA, mostra ao Matheus.**

### Fase 2 — Rebase do Bloco 1 sobre a main reconciliada (em worktree isolado)
2.1. Worktree isolado: `git worktree add .worktrees/bloco1-rebase main`.
2.2. Nele: `git cherry-pick 7b695ec` (o commit do Bloco 1) — provado EXIT 0, sem conflito.
     OU rebase da branch `fix/seguranca-isolamento` sobre a main nova.
2.3. Rodar a suite COMPLETA + `tsc` + `build` no worktree (agora com 1259+ testes, pois
     herda as features de prod + os 22 testes do Bloco 1).
2.4. ✅ Checkpoint: tudo verde. **PARA, mostra ao Matheus.**

### Fase 3 — Deploy (só com OK explícito do Matheus)
3.1. Backup tag de prod: `backup/prod-antes-bloco1-2026-06-25 → 7823443`.
3.2. Deploy do worktree (main+Bloco1) via Vercel CLI — **sem migrate** (Bloco 1 não tem
     migração; main=prod já tem as 56).
3.3. Smoke test: recibo/carnê (XSS), conciliação, fechamento de caixa, + rotas-chave 401/307/200.
3.4. ✅ Checkpoint final: smoke verde. Decisão do Matheus de mergear a branch na main
     (ou manter o padrão atual de deploy-da-branch).

### Tratamento das migrações divergentes (resumo)
- **Não rodar `migrate deploy` às cegas.** As 3 migrations já estão aplicadas no banco.
- A reconciliação por FF traz os ARQUIVOS das 3 migrations para a main, alinhando
  pasta↔banco. `migrate status` confirma a consistência **sem aplicar nada**.
- Se (e só se) o `migrate status` pós-FF apontar alguma pendência inesperada → **PARAR**
  e investigar antes de qualquer `migrate deploy`.

### Onde NÃO mexer
- Cockpit (MetricSample/AdminActionLog) — decisão separada do Matheus (§5), fora deste plano.

---

## O que NÃO consegui verificar / ressalvas
- **Confirmação do commit de prod via Vercel:** confiei no commit `7823443` informado
  pelo dashboard (Matheus confirmou). Não acessei o painel Vercel nesta sessão para
  reconferir o hash — se houver divergência, todo o diagnóstico deve ser refeito com o
  hash correto.
- **`migrate status` aponta para o banco do `.env` local** (que é o de prod/Neon). Assumi
  que é o banco de produção real. Se o `.env` apontasse para outro banco, a contagem de
  "aplicadas" mudaria.
- Não inspecionei o conteúdo linha-a-linha dos 50 commits de prod — confiei na
  ancestralidade linear (que prova matematicamente que prod ⊇ main, sem regressão).

---

## ✅ Confirmação final
**Nada foi alterado.** Nenhuma branch real (`main`, `fix/rotina-teste-21-06`,
`fix/seguranca-isolamento`) foi tocada. Nenhum merge, rebase, push, deploy ou migração.
Banco não foi escrito (só `migrate status`, que lê). O worktree temporário
`/tmp/drift-check` e a branch `tmp/drift-rebase-test` foram criados para simular o rebase
e **removidos**. Tudo somente leitura.
